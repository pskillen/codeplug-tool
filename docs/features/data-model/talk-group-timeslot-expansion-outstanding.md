# Talk group timeslot expansion — outstanding

**Tracking:** [codeplug-tool#142](https://github.com/pskillen/codeplug-tool/issues/142)

---

## Open

- **Slice 7:** `talkGroupMergeCandidates.ts`, `mergeTalkGroupsIntoOne`, store action `APPLY_TALK_GROUP_MERGES`
- **Slice 8:** `TalkGroupMergeCandidatesModal`, **Find merge candidates** on Talk Groups section nav
- **Slice 9:** `docs/reference/opengd77/multi-timeslot-talkgroups.md`, CRUD README, native YAML field table, close progress logs
- **PR:** push branch and open PR with `Closes #142` after slices 7–9 and `npm run format:check && npm run lint && npm run test && npm run build`
